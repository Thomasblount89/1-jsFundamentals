



let resource = "|==========|";

function machine(ironrod) {
    let screws;
}
     if(ironRod) {
     screws; = '|--- |--- |---';
} else { 
    console.log('not fed any resources');
}
return screws;

}

let powerON = machine(resource);
console.log(powerON);


// block body arrow function 
let myName = (fName , lname) => {
    let fullname = `${fname} ${lname}`;

    return fullname;
}

let powerON = myname('spongeBob,'Sqaurepants');
console.log(powerON);

// concise body arrow function - return happens by defualt with consie body arrow functions
let myName = (fname, lname) => `${fname} ${lname}`;

let powerOn = myname ('patrick', 'star');
console.log(powerOn);

// group challenge 

/*
CHALLENGE
************
    Make a tip calculator using a function
    Have it RETURN the value
    Capture that returned value in a VARIABLE
    Print that variable to the console
*/

//let insert here maybe? 
function tipcal(bill) {
        let tip =(bill*=.25);
        
}
let totalTip = tipcal(25.50);
console.log(tip)

// teachers example 

function tipcalc(bill){
    let tip = bill * 0.2
    return tip
}
let totaltip = tipcalc(25.50);
console.log(totaltip)